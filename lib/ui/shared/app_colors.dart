import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Main - Primary color variants
  static const Color primaryColor = Color(0xFF0ABF52);
  static const Color primaryColorDarkest = Color(0xFF06883B);
  static const Color primaryColorDark = Color(0xFF049941);
  static const Color white = Color(0xFFFFFFFF);
  static Color primaryColor80 = primaryColor.withOpacity(.8);
  static Color primaryColor55 = primaryColor.withOpacity(.55);
  static Color primaryColor40 = primaryColor.withOpacity(.40);
}
